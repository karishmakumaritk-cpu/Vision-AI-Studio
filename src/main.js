const app = document.querySelector('#app');

app.innerHTML = `
  <h1>Vision AI Studio</h1>
  <p>
    Deployment is now configured for Vercel. This Vite app provides the required
    <code>package.json</code> and build scripts so Vercel can run <code>npm install</code>
    and <code>npm run build</code> successfully.
  </p>
  <p class="pill">Ready for Vercel deployment</p>
`;
