PORT=5000
NODE_ENV=development
# Frontend app base URL (used for callbacks/links)
FRONTEND_URL=https://vision-ai-studio-git-main-velisions-projects.vercel.app
JWT_SECRET=replace_with_long_secret

# Supabase project URL (HTTPS) and service role key
SUPABASE_URL=https://pcrtgcyqryafuautfctg.supabase.co
SUPABASE_SERVICE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY

# PostgreSQL connection strings (do NOT commit real passwords)
# Recommended runtime (pooler) for app runtime (IPv4-friendly / pgbouncer):
# DATABASE_URL="postgresql://postgres.pcrtgcyqryafuautfctg:[YOUR-PASSWORD]@aws-0-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1&sslmode=require"

# Direct DB host (used only when needed, e.g. migrations):
# DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@db.pcrtgcyqryafuautfctg.supabase.co:5432/postgres?sslmode=require"

OPENAI_API_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Email / notification
GMAIL_USER=karishmakumaritk@gmail.com
GMAIL_APP_PASSWORD=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# NOTE: Do NOT put the full postgresql:// URI into SUPABASE_URL. SUPABASE_URL must be the HTTPS project URL.
# Use DATABASE_URL / DIRECT_URL for Prisma / migrations and runtime DB connections as appropriate.
