# Prisma / SQL clients (Supabase Postgres)
# IMPORTANT:
# - Replace <YOUR-PASSWORD> with your real DB password (find it in Supabase → Settings → Database → Connection string)
# - URL-encode special characters in password (@, :, /, #, ?, etc.)
# - Keep sslmode=require for Supabase
# - Use connection pooler URL for app runtime where possible

# Recommended runtime URL (Supabase pooler, IPv4-friendly)
DATABASE_URL="postgresql://postgres.pcrtgcyqryafuautfctg:<YOUR-PASSWORD>@aws-0-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1&sslmode=require"

# Required for Prisma migrations (direct connection, not pooler)
DIRECT_URL="postgresql://postgres:<YOUR-PASSWORD>@db.pcrtgcyqryafuautfctg.supabase.co:5432/postgres?sslmode=require"

# Set to your production URL on Vercel; use http://localhost:3000 for local dev
NEXTAUTH_URL="https://vision-ai-studio-git-main-velisions-projects.vercel.app"

# Generate with: openssl rand -base64 32
# Do NOT use your Vercel project ID here — it must be a long random secret
NEXTAUTH_SECRET="replace_with_output_of_openssl_rand_-base64_32"

GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
OPENAI_API_KEY=""
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
STRIPE_PRO_PRICE_ID=""
STRIPE_PREMIUM_PRICE_ID=""
NEXT_PUBLIC_APP_URL="https://vision-ai-studio-git-main-velisions-projects.vercel.app"

# Supabase auth
# NEXT_PUBLIC_SUPABASE_URL must be the Supabase project REST URL (https://<ref>.supabase.co)
# Do NOT set this to a PostgreSQL connection string — that belongs in DATABASE_URL only
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
NEXT_PUBLIC_API_URL=https://vision-ai-studio-backend.onrender.com
# Public merchant ID (safe to expose)
NEXT_PUBLIC_MERCHANT_ID=nukoKk23721220820057

# Backend (server-only) env vars — DO NOT expose to the browser or commit
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
